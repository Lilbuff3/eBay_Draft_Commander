## $(date +%Y-%m-%d) - Adding ARIA labels to icon-only buttons
**Learning:** Adding `aria-label` and `title` attributes to icon-only buttons (`size="icon"`) using standard Radix UI/Tailwind button components is an effective way to improve screen reader accessibility and provide helpful tooltips for mouse users without altering layout or CSS.
**Action:** Consistently apply this pattern to all `size="icon"` buttons across the application components.
