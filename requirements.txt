# Core
# Requires Python >=3.12,<3.13 (3.13 installs are missing project deps; see CLAUDE.md)
# Floors lowered 2026-06-27 to match the known-good installed set (see
# requirements.lock). The previous floors (e.g. flask>=3.1.3) were higher than
# what the app actually runs on, so `pip install -r requirements.txt` would have
# upgraded off the tested versions. Keep these in sync with requirements.lock.
flask>=3.0.3
requests>=2.32.3
click>=8.2.1
python-dotenv>=1.0.1

# AI
google-genai>=1.42.0

# Database
sqlalchemy>=2.0.23

# Real-time
flask-socketio>=5.6.0
simple-websocket>=1.0.0  # WebSocket transport for threading async_mode (see app/__init__.py)
eventlet>=0.40.4  # deprecated/no longer on the run path; kept pinned, safe to remove later

# Image Processing
pillow>=12.1.1
rembg>=2.0.67
